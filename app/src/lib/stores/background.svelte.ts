// Background colour for the map canvas.
// Stored here so the history snapshot can read it without
// reaching into MapCanvas local state.
export const background = $state({ hex: '#f4f4f5', alpha: 1 });
